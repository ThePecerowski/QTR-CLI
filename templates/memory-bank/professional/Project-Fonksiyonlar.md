# Project Fonksiyonlar - {{PROJECT_NAME}}

---

## Cekirdek API

```php
Router::get('/path', fn() => ...);
Router::post('/path', fn() => ...);
Router::get('/path/{id}', fn($id) => ...);

View::render('template', ['key' => 'value']);

Config::get('KEY');

$model->find($id);
$model->all();
$model->create($data);
$model->update($id, $data);
$model->delete($id);
```

---

## Proje Fonksiyonlari

| Dosya | Fonksiyon | Aciklama |
|-------|-----------|----------|
| - | - | - |
